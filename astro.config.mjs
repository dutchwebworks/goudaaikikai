import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://goudaikikai.dutchwebworks.nl',
    name: 'Gouda Aikikai &raquo; ',
    compressHTML: false,
    trailingSlash: 'never',
});
